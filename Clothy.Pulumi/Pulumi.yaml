name: Clothy.Pulumi
description: A minimal C# Pulumi program
runtime: dotnet
config:
  keycloakUrl:
    description: Keycloak server URL for realm configuration
    default: http://localhost:8080
  smtpFrom:
    description: SMTP From email address (must be verified sender in SendGrid)
  sendgridApiKey:
    description: SendGrid API key for email sending
    secret: true
  adminEmail:
    description: Admin user email address (must be verified sender in SendGrid)
  adminPassword:
    description: Admin user password
    secret: true
  managerPassword:
    description: Manager user password
    secret: true
  userPassword:
    description: Regular user password
    secret: true